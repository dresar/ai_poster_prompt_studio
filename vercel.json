{
  "buildCommand": "cd promtingfrontend && npm install && npm run build",
  "outputDirectory": "promtingfrontend/.output/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
